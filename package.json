{
  "name": "notes-api",
  "version": "1.0.0",
  "description": "A notes REST API built on Node.js core modules.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && cp -r public dist/public",
    "start": "node dist/server.js",
    "dev": "tsc && cp -r public dist/public && node dist/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.0.0",
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": ["nodejs", "rest-api", "typescript"],
  "license": "MIT"
}